v26.4.12-fork.11更新内容：

注意：从 fork.11 之前的版本升级，必须先卸载再重装；只点菜单6更新脚本不会注册 WARP 账户，也不会重建 sb10/sb11 的 wireguard 出站与分流规则。

更新内容：
1、恢复上游 WARP-WireGuard：安装时自动注册 Cloudflare WARP 账户（openssl+curl+jq，不依赖 python3/xxd）
2、恢复域名分流菜单（主菜单 3 → 6）：WARP-WireGuard-IPv4/IPv6 优先、VPS 本地 IPv4/IPv6 优先，共 4 个通道，双栈优先模式
3、服务端模板：sb11 使用 endpoints 的 wireguard 端点 + resolve/outbound 成对规则（10 条），sb10 保留旧版 wireguard 出站 + 分流出站规则（6 条）
4、仍然不做：WARP-Socks5/warp-plus、CFwarp 管理入口、WARP 端点 IP 优选、Argo、其它协议、GitLab/TG 推送、本地IP订阅
5、守卫同步：fork-check 新增 WARP-WireGuard 结构守卫（账户注册函数、两个模板的 wireguard 出站与分流规则、按 JSON 路径写入的分流菜单），变更配置菜单选项数改为 6；render-configs 校验 endpoints 与 resolve/outbound 成对规则

详见：https://github.com/jasper-khan/sing-box-yg
